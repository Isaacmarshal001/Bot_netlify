[build]
  publish = "public"
  functions = "netlify/functions"

# Scheduled daily message at 08:00 Nigeria Time (WAT = UTC+1)
[[functions]]
  name = "sendMessage"
  schedule = "0 7 * * *"   # 7 UTC = 8 AM Nigeria time
